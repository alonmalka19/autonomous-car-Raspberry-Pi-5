# Core dependencies
ultralytics>=8.0.0
opencv-python>=4.8.0
numpy>=1.24.0

# Hardware control
gpiozero>=2.0

# Web streaming
flask>=2.0.0
requests>=2.28.0

# ROS2 dependencies (install via apt)
# sudo apt install ros-jazzy-cv-bridge ros-jazzy-foxglove-bridge
